# PrestaShop Category FAQ
Module for category questions section 

## Versions

PrestaShop >= 1.7.x 

## Install
For module install follow this steps:
- Download and unzip archive or clone this repository
- Copy directory `ps_categoryfaq` in `modules` directory of your site
- Go to module list in your Control Panel and find module by phrase `"categoryfaq"` and enable it